# qa_python
#       !!!Новые правки!!!
#   1. "Здесь почему осталось обращение по ключу?" - изменила ассерт, решила убрать атрибутику 
#   2. "Нужно исправить: get_book_genre() это метод, в этой конструкции он не вызывается" - изменила ассерт, добавила вызов словаря, а не метода
#   3. "Нужно исправить: повторный коммент: здесь и далее: при наличии методов работы с атрибутом у тестируемой программы, необходимо использовать их,
#   нам важно знать что не только атрибут изменился но и метод работы с ним работает, так и тестов будет меньше и атомарность не пострадает" - изменила ассерт, убрала атрибут и добавила метод 
#   4. "Нужно исправить: повторный коммент: уже проверено в test_get_book_genre, копипастить не надо" - еще в первое исправление я удалила проверку test_get_book_genre под номером проверки 3.0. Оставила с параметризацией, прошу обратить внимание на это, также оставила комменатрий на гит хабе.




# !!! Удалила лишнюю часть кода, которая мною была закоменчена, которая дана была примером.!!!
# Метод test_add_new_book:
#   Проверка 1.0: Проверяем, что название книги корректно добавляется
#       Cоздать экземпляр класса, вызвать метод add_new_book с названием книги, которая еще не существует в коллекции. 
#       Проверить, что книга была добавлена успешно.
#   Проверка 1.1: (сделала отдельным тестом) Проверяем, что книга с таким же названием не добавляется повторно
#       Вызвать метод add_new_book с названием книги, которая уже существует в коллекции. 
#       Проверить, что книга не была добавлена повторно.

# Метод test_set_book_genre:
#   Проверка 2.0: Проверяем, что книге успешно присваивается жанр
#       Cоздать экземпляр класса, добавить книгу в коллекцию, вызвать метод set_book_genre с названием книги и жанром. 
#       Проверить, что жанр книги был установлен успешно.
#   Проверка 2.1: (сделала отдельным тестом) Проверяем, что жанр книги с неправильным названием не изменяется
#       Вызвать метод set_book_genre с несуществующим или некорректным названием книги и жанром. 
#       Проверить, что жанр книги не был изменен.

# Метод test_get_book_genre:
#   Проверка 3.0: (Тест на метод решила удалить, чтобы не дублировался и оставить один с параметризацией)

# Метод test_get_books_with_specific_genre:
#   Проверка 4.0: Проверяем, что метод возвращает правильный список книг с указанным жанром
#       Cоздать экземпляр класса, добавить несколько книг с разными жанрами в коллекцию, вызвать метод get_books_with_specific_genre с жанром, для которого есть книги. 
#       Проверить, что возвращается правильный список книг.

# Метод test_get_books_genre:
#   Проверка 5.0: Проверяем, что метод возвращает словарь books_genre
#       Создать экземпляр класса, добавить книги в коллекцию, установить жанр книг, вызвать метод get_book_genre с названиями книг. 
#       Проверить, что метод возвращает словарь books_genre.
# Метод test_get_books_for_children:
#   Проверка 6.0: Проверяем, что метод возвращает список книг, подходящих детям
#       Cоздать экземпляр класса, добавить несколько книг с разными жанрами в коллекцию, вызвать метод get_books_for_children. 
#       Проверить, что возвращается список книг без жанров с возрастным рейтингом.

# Метод test_get_books_for_children_if_genre_not_for_children:
#   Проверка 7.0: Проверяем, что метод возвращает пустой список, если не добавлено книг подходящих детям
#       Cоздать экземпляр класса, добавить книгу в коллекцию, установить жанр книги, входящий в список "Не для детей", вызвать метод test_get_books_for_children_if_genre_not_for_children.
#       Проверить, что метод возвращает пустой список, т.к. книга не для детей.
#

# Метод test_add_book_in_favorites:
#   Проверка 8.0: Проверяем, что книга успешно добавляется в избранное
#       Cоздать экземпляр класса, добавить книгу в коллекцию, вызвать метод add_book_in_favorites с названием книги. 
#       Проверить, что книга была успешно добавлена в избранное.
#   Проверка 8.1: (сделала отдельным тестом) Проверяем, что книга уже добавленная в избранное не добавляется повторно
#       Добавить книгу, уже добавленную в избранное, в коллекцию, вызвать метод add_book_in_favorites с названием книги. 
#       Проверить, что книга в избранное не добавляется повторно.

# Метод test_delete_book_from_favorites:
#   Проверка 9.0: Проверяем, что книга успешно удаляется из избранного
#       Cоздать экземпляр класса, добавить книгу в коллекцию и в избранное, вызвать метод delete_book_from_favorites с названием книги. 
#       Проверить, что книга была успешно удалена из избранного.
#   Проверка 9.1: (сделала отдельным тестом) Проверяем, что удаление несуществующей книги из избранного не вызывает ошибки
#       Вызвать метод delete_book_from_favorites с названием книги. 
#       Проверить, что удаление книги не вызвало ошибку.

# Метод test_get_list_of_favorites_books:
#   Проверка 10.0: Проверяем, что метод возвращает список избранных книг
#       Cоздать экземпляр класса, добавить несколько книг в коллекцию и в избранное, вызвать метод get_list_of_favorites_books. 
#       Проверить, что возвращается правильный список избранных книг.

# Метод test_add_new_book_duplicate:
#   Проверка 11.0:  Тест для проверки отсутствия добавления книги в коллекцию, если книга с таким названием уже существует: 
#       Cоздать экземпляр класса, добавить книгу в коллекцию, вызвать метод add_new_book с тем же названием книги. 
#       Проверить, что книга не была добавлена повторно.

# Метод test_set_book_genre_parametrized:
#   Проверка 12.0: Параметризованный тест для метода get_books_with_specific_genre. 
#       Написать декоратор и ввести параметры "Название" и "Жанр", создать экземпляр класса, добавить книгу за счет паремтров add_new_book, добавить жанр за счет паремтров set_book_genre, вызвать метод get_book_genre
#       Проверяем, что метод возвращает список книг с указанным жанром.